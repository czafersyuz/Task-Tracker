import React from 'react'

const Header = ({title}) => {
  return (
    <div>
        <h1>{title}</h1>
    </div>
  )
}
// Header.defaultProps ={
//     title:"task tarcker"
// };

//title ="task default" const headerin içine yazılır

export default Header